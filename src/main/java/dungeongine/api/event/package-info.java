/**
 * {@link dungeongine.api.event.Event}s (and the {@link dungeongine.api.Events} class) allow plugins to modify actions
 * at runtime. For example, a plugin might listen for {@link dungeongine.api.event.player.PlayerJoinEvent} and change
 * the message to a less formal greeting, or cancel the event to prevent the player from joining the server altogether.
 */
package dungeongine.api.event;